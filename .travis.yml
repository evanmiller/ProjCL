language: c
sudo: required
os:
    - osx
    - linux

addons:
    apt:
        packages:
            - libblas-dev
            - liblapack-dev
            - ocl-icd-opencl-dev
            - alien

install:
    - ./.travis/install-proj4.sh
    - ./.travis/install-opencl.sh

script:
    - cmake .
    - make
    - ./test/projcl_test -CPU
